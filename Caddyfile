gengrenpeng.cn, www.gengrenpeng.cn {
	# 开启反向代理
	reverse_proxy * localhost:3000
}

# MeiliSearch 反向代理
meilisearch.gengrenpeng.cn, meilisearch.www.gengrenpeng.cn {
	# 开启反向代理
	reverse_proxy * localhost:8443
}
